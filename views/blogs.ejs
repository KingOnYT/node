<!DOCTYPE html>

  <body>
 
<%-  include("./componets/head.ejs") %>      
<%-  include("./componets/nav.ejs") %> 
      <div class="blogs">
    <% if(blogs.length > 0){%> <% blogs.forEach(blog => {%>
        <h3 class="title"><%= blog.title%></h3>
        <p class="snippets"><%= blog.snippet%></p>
        <hr>

        <%})%>
        <h6>Yay you reached the end!</h6>
        <%}else {%>
        <p>No Blogs to display!</p>
        <% } %>
    </div>
  </body>
</html>
